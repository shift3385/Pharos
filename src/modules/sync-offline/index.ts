// sync-offline module boundary (spec §9). Reserved from day 1; implemented in
// Phase 8 (delta oplog, auto + manual sync, conflict panel, status indicator).
export const SYNC_OFFLINE_MODULE = "sync-offline" as const;
